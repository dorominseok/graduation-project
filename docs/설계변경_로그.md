# 설계 변경 로그
### 운동 습관 분석 기반 맞춤 루틴 추천 헬스 웹앱

> 계획서 제출 이후 설계·구현 과정에서 변경된 사항을 기록한다.
> 각 항목은 **기존 버전 / 기존 버전의 문제점 / 수정 버전 / 수정한 이유와 방식** 의 네 부분으로 정리한다.
> 최종 보고서의 "계획과 달라진 부분" 작성 근거로 사용한다.

---

## 목록

| ID | 변경 항목 | 발생 시점 | 영향 문서 |
|---|---|---|---|
| [LOG-01](#log-01) | 운동 기록 데이터 구조 (단일 테이블 → 세션·세트 2계층) | 2026-08-02 | 계획서 4.4, 기능명세서, 유스케이스 정의서 |
| [LOG-02](#log-02) | 세트 집계 시 워밍업 세트 제외 | 2026-08-02 | 분석 로직 설계서 1.2·2.3, 기능명세서 2.2 |
| [LOG-03](#log-03) | 운동 시간 기록 방식 확정 (자동 산출) | 2026-08-02 | 기능명세서 2.6 |
| [LOG-04](#log-04) | 운동 종목 데이터에 측정 유형(measure_type) 추가 | 2026-08-02 | 계획서 4.4, 분석 로직 설계서 8장 |
| [LOG-05](#log-05) | 루틴 계획 세트의 저장 시점 확정 | 2026-08-02 | 기능명세서 5장 |
| [LOG-06](#log-06) | 기록 수정·누락 처리 방침 확정 | 2026-08-02 | 유스케이스 정의서 UC-02 |
| [LOG-07](#log-07) | 추정 1RM 산출 대상 세트 제한 | 2026-08-02 | 분석 로직 설계서 1.3 |
| [LOG-08](#log-08) | 운동 종목 데이터에 기구 정보(equipment) 추가 | 2026-08-04 | 기능명세서 2.4, 유스케이스 UC-03-05, 기록 방식 설계서 2.3 |
| [LOG-09](#log-09) | 판정 부위 단위 정의 (body_part 6종 → primary_muscle 기반 9종) | 2026-08-08 | 운동 분석 로직 설계서 2장·4장, 계획서 4.3·4.4 |
| [LOG-10](#log-10) | 판정·표시 부위의 2계층 구성 (표시 6종 / 판정 9종) | 2026-08-29 | 운동 분석 로직 설계서 2장·5장, 기능명세서 2.2 |
| [LOG-11](#log-11) | 프로필 항목에서 운동 경력(experience_level) 제외 | 2026-08-30 | 기능명세서 1장, 유스케이스 UC-01-03, API 명세서 2.4·4.5·4.6 |
| [LOG-12](#log-12) | 목업 전수 대조로 확정한 미규정 사항 (하루 세션 수·주 시작 요일·setNo·경과 시간·추이 API) | 2026-08-31 | API 명세서 1.2·5.2·6.1·6.3·6.12·9.1·9.2, 기능명세서 1.1, 기록 방식 설계서 1장·2.1 |
| [LOG-13](#log-13) | 구현 착수 전 계약 검토로 확정·정정한 사항 (프리필 API·집계 구간·판정 경계·멱등 키·오프라인·휴식 타이머) | 2026-09-01 ~ 09-02 | API 명세서 3.1·3.2·4.7·5.5·6.2·6.4·6.5·6.6·8.2·9.1, 기록 방식 설계서 2.2·3.4·3.7·7장·8장, 분석 로직 설계서 2.1·2.2, 기능명세서 2.2, UC-03-01, 구축 기록 11.2 |
| [LOG-14](#log-14) | 프로필 항목을 훈련 목표 1종으로 축소 (키·체중 제외) | 2026-09-02 | API 명세서 2.4·4.5·4.6·9.1·9.2, 기능명세서 1장, 유스케이스 UC-01-03, `V3` 마이그레이션 |
| [LOG-15](#log-15) | 배포 시점을 11월에서 10월 중순으로 조정 | 2026-09-02 | 개발환경 구축 기록 5.1·11장·13장, 기능명세서 5장(검증 계획) |

---

<a id="log-01"></a>
## LOG-01. 운동 기록 데이터 구조

### 기존 버전

계획서 4.4의 주요 엔티티 목록에 다음과 같이 기재되어 있었다.

| 엔티티 | 설명 |
|---|---|
| workout_logs | 운동 기록(사용자·종목·중량·세트·횟수·일자) |

즉 운동 기록을 단일 테이블로 두고, 한 행이 하나의 기록을 나타내는 구조였다.

### 기존 버전의 문제점

기록을 세션 단위로 묶는 개념이 없어, 다음 세 가지를 산출할 수 없었다.

1. **총 운동 시간** — 친구 그룹 활동 공유(기능명세 2.6)는 "멤버별 주간 운동 횟수와 총 운동 시간"을 표시하기로 되어 있으나, 개별 기록 행만으로는 "한 번의 운동"이 어디서 시작해 어디서 끝났는지 알 수 없다.
2. **주간 운동 횟수** — 같은 날 여러 종목을 기록하면 행이 여러 개 생기므로, 행 수로는 운동 횟수를 셀 수 없다. 날짜 중복 제거로 근사할 수는 있으나 개념이 불명확하다.
3. **루틴 실제 수행률** — 계획서 7장의 실사용자 검증 항목인 "추천 루틴 실제 수행률(사용 로그 기반 자동 측정)"은 "이번 운동이 어느 추천 루틴에서 시작되었는가"의 연결을 요구하는데, 이를 걸 대상이 없었다.

또한 세트를 한 행에 묶어 저장하는지 세트마다 나누어 저장하는지가 명확하지 않아, 세트별 강도(%) 표시(분석 설계서 1.3)의 구현 가능 여부가 불확실했다.

### 수정 버전

기록을 **세션 — 세트** 2계층으로 분해하였다.

| 엔티티 | 설명 |
|---|---|
| workout_sessions | 한 번의 운동. 수행 일자, 루틴 연결, 세션 상태, 운동 시간. 하루 여러 세션 허용(2026-08-31 확정) |
| workout_sets | 세트 1개 = 1행. 종목, 세트 순번, 중량, 횟수, 워밍업 여부, 저장 시각 |

컬럼 정의는 「운동기록_방식_설계서.md」 2장에 정리하였다.

### 수정한 이유와 방식

**이유**: 계획서 단계에서는 "운동 기록을 저장한다"는 수준으로만 기술했으나, 지도교수 피드백에 따라 기록 방식을 구체화하는 과정에서 **분석·그룹·검증 기능이 실제로 요구하는 데이터를 역산**한 결과 단일 테이블로는 충족되지 않음이 드러났다.

**방식**: 각 기능이 필요로 하는 데이터를 표로 정리한 뒤(설계서 1장), 그 요건을 모두 만족하는 최소 구조를 도출하였다. 세션 개념은 운동 시간·주간 횟수·수행률 세 가지를 한꺼번에 해결하므로 추가 비용 대비 효용이 크다고 판단하였다.

**주의**: 데이터 구조는 기록이 쌓인 뒤에는 되돌리기 어렵다. 9월 구현 후 10월에 문제를 발견하면 이미 쌓인 데이터를 복구할 수 없으므로, 구현 착수 전 설계 단계에서 확정하였다.

**계획서 반영 여부**: 반영하지 않는다. 계획서는 제출·승인이 완료되어 계획 시점의 기록으로 두고, 변경 사항은 본 로그와 최종 보고서에 담는다.

---

<a id="log-02"></a>
## LOG-02. 세트 집계 시 워밍업 세트 제외

### 기존 버전

「운동 분석 로직 설계서」 1.2에서 세트 카운팅 기준을 다음과 같이 정하였다.

> 한 운동이 여러 근육을 사용하더라도, 해당 운동의 주동근에만 세트를 집계한다.

즉 **어느 근육에 세트를 배정할 것인가**만 정하고, **어떤 세트를 셀 것인가**는 정하지 않았다. 사실상 기록된 모든 세트를 세는 것과 같았다.

### 기존 버전의 문제점

1. **기준값과 계산 단위의 불일치.** ACSM의 "부위별 주 4/10~20세트" 기준은 실패 근처까지 수행하는 본세트를 센 값이다. 워밍업을 함께 집계하면 앱의 계산 단위가 기준값의 단위와 달라지므로, "4세트 미만이면 부족"이라는 판정의 근거가 성립하지 않는다. 설계서 0장이 내건 "기준값은 문헌에서, 계산 방법은 직접 설계한다"는 원칙에서, 계산 방법이 기준값과 어긋나는 경우이다.
2. **오판의 방향이 나쁘다.** 워밍업을 포함하면 볼륨이 부풀려져 실제로 부족한 부위가 "권장 이하"나 "최적"으로 올라온다. 즉 **약점을 놓치는 방향**으로 틀리는데, 이는 "사용자가 인지하지 못하는 약점을 짚어 준다"는 이 앱의 존재 이유를 직접 훼손한다.
3. **균형 판정도 왜곡된다.** 워밍업은 부위에 고르게 분포하지 않고 스쿼트·벤치프레스 같은 고중량 복합운동에 집중되므로, 밀기·하체 볼륨이 상대적으로 과대 계상되어 2배 임계 판정에 영향을 준다.

### 수정 버전

- `workout_sets`에 `is_warmup` 불리언 컬럼을 두고, 기본값을 false로 한다.
- 부위별 볼륨 집계 시 `is_warmup = false`인 세트만 센다.
- 기록 화면에 워밍업 토글을 제공하되, "본세트만 기록해도 무방하다"고 안내한다.

### 수정한 이유와 방식

**이유**: 위 세 가지 문제 중 특히 2번이 결정적이었다. 판정이 틀리더라도 "과하다고 잘못 알리는" 방향이면 사용자가 확인 후 넘어갈 수 있지만, "부족한데 충분하다고 알리는" 방향은 사용자가 문제를 인지할 기회 자체를 없앤다.

**방식**: 세 가지 선택지를 비교하였다.

| 선택지 | 판단 |
|---|---|
| 구분 없이 전부 카운트 | 구현이 가장 단순하나 위 문제를 그대로 안는다 |
| 본운동만 기록하도록 안내 | 안내에만 의존하므로 실효성이 없고, 워밍업을 기록하려는 사용자를 막게 된다 |
| **토글 제공 + 집계 제외 (채택)** | 불리언 컬럼 하나와 집계 조건 한 줄. 비용 대비 정합성 확보 효과가 크다 |

**한계 인지**: 사용자가 워밍업을 표시하지 않으면 여전히 과대 집계된다. 자동 판별(그날 최고 중량의 일정 비율 미만인 선행 세트를 워밍업으로 간주하는 방식)도 검토했으나, 임계 비율의 근거를 대기 어렵고 오탐 시 사용자가 이유를 이해할 수 없어 채택하지 않았다. 설계서 0장의 "과한 정밀도를 피한다"는 원칙에 따른 판단이며, 확장 아이디어로 기록해 두었다.

---

<a id="log-03"></a>
## LOG-03. 운동 시간 기록 방식

### 기존 버전

기능명세서 2.6에 그룹 활동 공유 항목으로 "주 운동 횟수, 총 운동 시간"을 표시한다고만 기재되어 있었다. 이 시간을 **어떻게 얻을 것인지**는 정해지지 않았고, 「운동 분석 로직 설계서」 7.2에서 검토 항목으로만 남아 있었다.

### 기존 버전의 문제점

방식을 정하지 않으면 구현 단계에서 임기응변으로 결정하게 되는데, 어느 방식을 택하든 데이터 구조에 영향을 준다.

- 명시적 시작/종료 버튼 방식 → 세션 테이블에 시작·종료 시각이 필요
- 세트 시각 기반 자동 산출 → 세트마다 저장 시각이 필요
- 사용자 직접 입력 → 세션에 입력값 컬럼이 필요

즉 이 결정이 늦어지면 데이터 구조를 확정할 수 없다. 또한 이 값은 그룹 멤버에게 공개되므로 이상치 방어가 필요한데, 그 규칙도 함께 정해야 했다.

### 수정 버전

**세트 저장 시각 기반 자동 산출**로 확정하였다.

```
duration_sec = Σ(연속한 두 세트의 간격, 단 15분 초과 시 15분으로 캡)
             + 90초 (마지막 세트 수행 시간 보정)
상한: 세션당 4시간
```

- 사후 입력(BACKFILL) 세션은 저장 시각이 실제 운동 시각과 무관하므로 시간을 산출하지 않고, 그룹의 총 시간 집계에서 제외한다. 단 주간 운동 **횟수** 집계에는 포함한다.
- `duration_override_sec` 컬럼을 미리 확보해 두고, 수동 보정 UI는 필요 시 추가한다.

### 수정한 이유와 방식

**이유**: 세 가지 방식을 비교한 결과이다.

| 방식 | 문제 |
|---|---|
| 명시적 시작/종료 버튼 | 운동 중에 종료를 누르는 것을 잊기 쉽다. 잊으면 데이터가 유실되거나 비정상적으로 길어진다 |
| 사용자가 총 시간 직접 입력 | 추가 입력 부담이 생기고 값이 부정확하다 |
| **세트 시각 자동 산출 (채택)** | 추가 조작이 전혀 없다. 세트 기록이라는 이미 하는 행위에서 부수적으로 얻어진다 |

이 값은 그룹 표시용이므로 분 단위 정밀도면 충분하다는 점도 고려하였다. 정밀도 요구가 낮은 값을 위해 사용자에게 조작을 요구하는 것은 비용 대비 효용이 낮다.

**방식**: 자동 산출의 약점은 이상치이므로 보정 규칙을 함께 설계하였다. 앱을 닫고 나갔다가 몇 시간 뒤 마지막 세트를 입력하는 경우가 실제로 흔하므로, 세트 간격에 상한(15분)을 두어 이탈 구간이 운동 시간에 포함되지 않게 하였다. 15분은 일반적인 세트 간 휴식(1~5분)보다 충분히 크되 이탈로 볼 만한 수준으로 설정하였다.

**보류 항목**: `duration_override_sec` 컬럼은 만들되 보정 UI는 만들지 않는다. 실사용자 배포 후 시간이 부정확하다는 피드백이 나올 가능성에 대비해, 스키마 변경 없이 화면만 추가할 수 있도록 자리를 남겨 둔 것이다.

---

<a id="log-04"></a>
## LOG-04. 운동 종목 데이터에 측정 유형 추가

### 기존 버전

계획서 4.4와 「운동 분석 로직 설계서」 6장에서, 운동 종목 데이터(exercises)는 free-exercise-db를 기반으로 **운동명(한글) + 주동근 + 밀기/당기기 구분**을 확보하는 것으로 계획되어 있었다.

### 기존 버전의 문제점

기록 화면을 설계하면서, 종목마다 입력해야 할 항목이 다르다는 점이 드러났다.

- 푸시업·크런치 같은 맨몸운동에는 중량 입력이 의미가 없다
- 플랭크 같은 시간 기반 종목에는 횟수 입력이 의미가 없다
- 중량 딥스·중량 턱걸이는 추가 중량만 기록되므로 추정 1RM 산출에 넣으면 값이 왜곡된다

이 구분이 데이터에 없으면 모든 종목에 동일한 입력 폼을 제공하게 되어, 입력 부담이 늘고 의미 없는 값(중량 0 등)이 저장되어 분석 데이터가 오염된다.

### 수정 버전

`exercises`에 `measure_type` 필드를 추가한다.

| measure_type | 입력 항목 | 예시 | 1RM 추정 |
|---|---|---|---|
| WEIGHT_REPS | 중량 + 횟수 | 벤치프레스, 스쿼트 | 대상 |
| BODYWEIGHT_REPS | 횟수 | 푸시업, 크런치 | 제외 |
| WEIGHTED_BODYWEIGHT | 추가 중량 + 횟수 | 중량 딥스 | 제외 |
| TIME | 시간(초) | 플랭크 | 제외 |

### 수정한 이유와 방식

**이유**: 이 필드가 없으면 기록 화면을 종목별로 분기할 수 없고, 추정 1RM 산출 대상을 걸러낼 수도 없다.

**방식**: free-exercise-db에는 이 정보가 없으므로 **8월 2주 운동 종목 DB 정제 작업에 포함**한다. 이미 한글 명칭과 부위 매핑을 수작업으로 정제할 예정이므로, 같은 작업에 필드 하나를 더 채우는 형태로 흡수한다. 별도 작업으로 분리하면 종목 목록을 두 번 훑게 되어 비효율적이다.

**일정 영향**: 계획서 8장 (3)에서 종목 데이터 정제 시간을 위험 요소로 인식하고 "자주 쓰이는 종목을 우선 정제"하는 대응을 두었다. 이 대응은 그대로 유효하며, 우선 정제 대상 종목에 `measure_type`을 함께 채우면 된다.

---

<a id="log-05"></a>
## LOG-05. 루틴 계획 세트의 저장 시점

### 기존 버전

추천 루틴을 받아 운동을 수행하는 흐름에서, 루틴의 계획 세트가 언제 기록으로 저장되는지가 정해져 있지 않았다. 기능명세서 2.1은 루틴 생성까지만 다루고, 그 루틴을 수행하며 기록하는 과정은 별도로 기술되지 않았다.

### 기존 버전의 문제점

루틴 진입 시점에 계획된 세트를 모두 저장하는 방식(구현하기 쉬운 방식)을 택하면 심각한 문제가 발생한다.

> 예: 등 3세트를 계획한 루틴에서 1세트만 수행하고 이탈한 경우, 등 볼륨이 3세트로 집계된다.

즉 **수행하지 않은 운동이 수행한 것으로 기록**되며, 이는 LOG-02와 같은 방향의 오류(약점을 놓치는 방향)이다. 게다가 추천 루틴을 따를수록 오차가 커지므로, 앱을 성실히 사용하는 사용자일수록 판정이 부정확해지는 역전이 발생한다.

또한 이 방식에서는 실제 수행률을 측정할 수 없다. 계획과 기록이 항상 일치하기 때문이다.

### 수정 버전

- 루틴에서 진입한 경우 계획 세트가 화면에 행으로 표시되지만, 이는 **화면 상태(계획)일 뿐 저장된 기록이 아니다.**
- 사용자가 **완료 체크를 하는 순간에만** `workout_sets` 행을 INSERT 한다.
- 따라서 `is_completed` 같은 별도 컬럼이 필요 없다. 행의 존재 자체가 "수행함"을 의미한다.

이로부터 수행률이 자연스럽게 산출된다.

```
수행률 = 세션에 실제 저장된 세트 수 ÷ 연결된 루틴이 계획한 세트 수
```

### 수정한 이유와 방식

**이유**: 기록 데이터의 무결성 문제이다. 분석 로직이 아무리 정확해도 입력 데이터가 사실과 다르면 결과가 무의미하다.

**방식**: "계획"과 "기록"을 저장 위치로 분리하였다. 계획은 `routines` / `routine_items`에, 기록은 `workout_sessions` / `workout_sets`에 있으며, 둘은 세션의 `routine_id`로만 연결된다. 이 분리 덕분에 계획서 7장의 실사용자 검증 항목인 "추천 루틴 실제 수행률(사용 로그 기반 자동 측정)"이 **별도 로깅 없이** 두 테이블의 세트 수 비교만으로 산출된다.

**부수 결정**: 루틴 후보정(기능명세 2.4)으로 운동을 교체한 경우, 수행률의 분모는 후보정 **이후**의 계획을 사용한다. 사용자에게 실제로 제시된 계획이 후보정된 루틴이기 때문이다.

---

<a id="log-06"></a>
## LOG-06. 기록 수정·누락 처리 방침

### 기존 버전

유스케이스 정의서 UC-02-02에 "운동 기록 수정 / 삭제"가 있었으나, 흐름과 처리 방침은 정의되지 않았다. 운동 중 앱을 벗어나는 경우, 과거 기록을 나중에 입력하는 경우, 기록을 삭제했을 때 이미 지급된 뱃지를 어떻게 할지는 정해진 바가 없었다.

### 기존 버전의 문제점

1. **미완료 세션**: 운동 중 앱을 닫는 것은 예외가 아니라 일상이다. 처리 방침이 없으면 기록이 유실되거나, 며칠 뒤 열었을 때 이전 세션에 세트가 추가되는 등의 오동작이 생긴다.
2. **기록 누락**: 운동은 했으나 기록하지 못한 경우를 보정할 수단이 없으면, 분석이 이를 "볼륨 부족"으로 판정한다. 판정 정확도가 사용자의 기록 성실성에 전적으로 좌우된다.
3. **수정 후 재집계**: 기록을 수정했을 때 이미 산출된 분석 결과를 어떻게 갱신할지 정해져 있지 않았다.
4. **뱃지와 기록 삭제**: 3대 500 뱃지를 받은 뒤 해당 기록을 삭제하면 뱃지를 회수할지 여부가 불분명했다. 구현 중에 마주치면 애매해지는 지점이다.

### 수정 버전

| 상황 | 처리 |
|---|---|
| 미완료 세션 | 세션에 DRAFT / DONE 상태를 둔다. 재진입 시 "이어서 기록 / 종료" 선택. 자정 경과 시 마지막 세트 시각 기준으로 자동 완료. **분석은 DONE 세션만 집계** |
| 과거 기록 입력 | 날짜를 지정한 사후 입력(BACKFILL)을 허용한다. 볼륨 집계에는 포함하고, 운동 시간 집계에서만 제외 |
| 수정·삭제 | 허용한다. 분석을 **온디맨드 집계**로 구현하여 수정이 자동 반영되게 한다 |
| 뱃지 | 이미 지급된 뱃지는 **회수하지 않는다** |
| 기록 희박 | 최근 4주 완료 세션이 임계(예: 6회) 미만이면 "판정 신뢰도 낮음"을 함께 표시 |

### 수정한 이유와 방식

**미완료 세션 — DONE만 집계하는 이유**: 진행 중인 세션이 판정에 섞이면, 운동 도중 루틴 추천을 요청할 때마다 결과가 달라진다. 오늘의 루틴은 "그때까지 쌓인 기록"을 근거로 하므로, 완료된 것만 근거로 삼아야 결과가 안정적이다.

**사후 입력을 허용하는 이유**: 직관적으로는 "정확한 실시간 기록만 받는 것"이 데이터 품질에 좋아 보이지만, 실제로는 반대다. 누락된 운동은 분석에서 부족으로 오판되므로, **누락을 방치하는 것이 부정확한 사후 입력보다 나쁘다.** 정확도보다 누락 방지를 우선한 선택이며, 대신 시간 데이터만 신뢰도가 낮으므로 그 항목에서만 제외하였다.

**온디맨드 집계를 택한 이유**: 집계 결과를 미리 저장해 두면 기록 수정 시 재계산 로직이 필요하고, 갱신 누락으로 화면마다 값이 다른 문제가 생긴다. 개인당 최근 4주 기록이 수백~수천 행 규모여서 매번 집계해도 성능 문제가 없으므로, 단순한 쪽을 택하였다. 통계 화면이 느려지는 경우에 한해 일별 집계 테이블 도입을 검토한다(현 시점 보류).

**뱃지를 회수하지 않는 이유**: 회수하려면 "어느 시점의 어떤 기록이 그 뱃지의 근거였는가"를 추적해야 하므로 구현 비용이 크다. 뱃지는 달성 순간의 기록이라는 성격이 강하고 프로필 전시용이므로, 엄밀한 정합성보다 단순함을 택하였다.

---

<a id="log-07"></a>
## LOG-07. 추정 1RM 산출 대상 세트 제한

### 기존 버전

「운동 분석 로직 설계서」 1.3은 추정 1RM 방식을 채택하며 다음과 같이 한계를 인지하였다.

> 추정 1RM은 공식 기반 추정치이며, 특히 고반복 구간에서 오차가 커진다. 따라서 정확한 1RM이 아니라 강도 경향 파악 용도로 사용한다.

그러나 한계를 인지했을 뿐, 그에 대한 처리는 정해두지 않았다. 모든 세트를 추정 계산에 넣는 것과 다름없었다.

### 기존 버전의 문제점

오차가 크다는 것을 알면서 오차가 큰 데이터를 그대로 사용하는 상태였다. 특히 20회 이상의 고반복 세트는 추정값이 실제와 크게 벌어지는데, 이 값이 그대로 추정 1RM 추이 그래프에 반영되면 사용자에게 잘못된 성장 추세를 보여주게 된다. 세트별 강도(%)는 추정 1RM을 분모로 쓰므로 그 값까지 함께 왜곡된다.

또한 하루에 같은 종목을 여러 세트 수행했을 때 어느 값을 그날의 대표값으로 삼을지도 정해져 있지 않았다.

### 수정 버전

- 중량·횟수를 기록하는 종목(`measure_type = WEIGHT_REPS`)의 세트만 대상으로 한다.
- **`반복 횟수 ≤ 12`인 세트만** 추정 계산에 사용한다.
- 하루에 같은 종목의 세트가 여럿이면 각 세트 추정값 중 **최댓값**을 그날의 추정 1RM으로 채택한다.

### 수정한 이유와 방식

**이유**: 인지한 한계를 실제 처리로 옮긴 것이다. 오차가 큰 구간을 사후에 보정하기보다, 애초에 계산에 넣지 않는 편이 단순하고 방어하기 쉽다.

**방식**: 12회를 경계로 삼은 것은 Epley 등 추정 공식이 대체로 10~12회 이내 구간에서 실측과 근접하다고 알려져 있기 때문이다. 다만 이 값 자체는 정밀한 근거를 갖는 수치가 아니므로, 설정값으로 두어 조정 가능하게 한다.

**부수 효과**: 워밍업 세트는 중량이 낮아 추정값이 최댓값이 되지 않으므로, 최댓값 채택 규칙에 의해 자동으로 배제된다. LOG-02의 워밍업 처리와 별도의 조건을 걸 필요가 없다.

---

<a id="log-08"></a>
## LOG-08. 운동 종목 데이터에 기구 정보 추가

### 기존 버전

운동 종목 데이터(`exercises`)에 확보하기로 한 항목은 다음과 같았다.

- 운동명(한글) + 주동근 + 밀기/당기기 구분 (계획서 4.4, 분석 로직 설계서 8장)
- 측정 유형 `measure_type` (LOG-04에서 추가)

한편 기능명세 2.4의 루틴 후보정은 운동 교체 시 **"같은 부위의 다른 운동 후보를 제시"** 한다고만 기술되어 있었다.

### 기존 버전의 문제점

후보 선정 기준이 `body_part` 하나뿐이면, 사용자가 운동을 교체하려는 **실제 이유를 해소하지 못하는 후보**가 제시된다.

기능명세 1장과 2.4는 "부상 부위·보유 기구 등 개별 제약은 입력받지 않고 후보정으로 조정한다"는 방침을 명시하고 있다. 그런데 교체를 원하는 상황은 대부분 다음과 같다.

- 해당 기구가 사용 중이거나 그 시설에 없다
- 부상 때문에 특정 동작을 피해야 한다

바벨 스쿼트를 교체하려는 사용자에게 같은 하체 운동이라며 **다시 바벨 종목**을 제시하면, 교체 기능이 의도한 역할을 하지 못한다. 즉 후보정이 제약 미입력 방침을 보완하는 장치로 설계되었음에도, 정작 후보를 고르는 데 필요한 정보가 데이터에 없었다.

### 수정 버전

`exercises`에 `equipment` 필드를 추가한다.

| equipment | 예시 종목 |
|---|---|
| BARBELL | 벤치프레스, 바벨 스쿼트 |
| DUMBBELL | 덤벨 컬, 덤벨 플라이 |
| MACHINE | 랫풀다운, 레그프레스 |
| CABLE | 케이블 크로스오버 |
| BODYWEIGHT | 푸시업, 크런치 |
| PULLUP_BAR | 턱걸이 |

루틴 후보정의 대체 운동 후보는 **같은 `body_part`이면서 `equipment`가 다른 종목을 우선 제시**한다.

### 수정한 이유와 방식

**이유**: 후보정 기능의 실효성 문제이다. 후보정은 이 앱이 사용자 제약을 입력받지 않기로 한 결정(기능명세 1장)을 지탱하는 장치이므로, 이것이 제 역할을 못 하면 제약 미입력 방침 자체가 약해진다.

**방식**: 세 가지 선택지를 비교하였다.

| 선택지 | 판단 |
|---|---|
| 사용자에게 보유 기구를 입력받는다 | 진입장벽을 낮춘다는 기능명세 1장의 방침과 정면으로 충돌한다 |
| 부위만으로 후보를 제시한다 | 구현은 단순하나 교체 사유를 해소하지 못한다 |
| **종목에 기구 정보를 부여한다 (채택)** | 필드 하나로 후보 선정의 실효성을 확보한다. 사용자 입력은 늘지 않는다 |

**시점을 8월 2주로 잡은 이유**: `equipment`가 실제로 쓰이는 것은 10월 루틴 추천 구현 시점이다. 그럼에도 지금 확보하는 것은 비용의 비대칭 때문이다.

| | 지금 추가 | 10월에 추가 |
|---|---|---|
| 스키마 | 컬럼 1개 | 마이그레이션 (경미) |
| **데이터** | 정제 시 필드 1개 추가 매핑 | **800여 종목 재검토** |

free-exercise-db 원본에 `equipment` 필드가 이미 존재하므로, 정제 과정에서 한글 매핑만 수행하면 된다. `measure_type`처럼 값을 새로 만들어야 하는 항목이 아니어서 추가 부담이 더 작다. LOG-04에서 `measure_type`을 같은 논리로 정제 작업에 흡수시킨 것과 동일한 판단이다.

**미확정 사항**: 후보정의 트리거 방식(사용자가 교체 버튼을 누르는가, 시스템이 자동 판단하는가)과 대체 후보의 정렬 기준은 10월 루틴 추천 설계에서 확정한다. 본 로그는 그때 필요한 데이터를 미리 확보하는 결정만을 다룬다.

**한계 인지**: 기구 분류가 다르다고 해서 반드시 대체 가능한 운동인 것은 아니다. 예컨대 부상으로 특정 동작 자체를 피해야 하는 경우에는 기구를 바꿔도 해결되지 않는다. 후보를 제시할 뿐 최종 선택은 사용자가 한다는 2.4의 성격("확정된 정답이 아니라 근거 있는 초안")은 그대로 유지된다.

---

<a id="log-09"></a>
## LOG-09. 판정 부위 단위 정의

### 기존 버전

「운동 분석 로직 설계서」 2장은 부족 판정의 임계값(주 4/10~20세트), 집계 기간(최근 4주 평균), 집계 조건(완료 세션의 본세트), 카운팅 원칙(주동근 기준)을 모두 확정하였다.

그러나 **그 "부위"가 무엇이며 몇 개인지는 어느 문서에도 정의되어 있지 않았다.** 계획서 4.3 역시 "부위별 집계한 뒤"라고만 기술하고 목록을 제시하지 않았다.

### 기존 버전의 문제점

**1. 저장용 분류를 판정 단위로 오인한 채 작업이 진행되었다.**

계획서 4.4의 `exercises` 테이블 설명에 등장하는 `body_part` 6종(CHEST / BACK / LEGS / SHOULDERS / ARMS / CORE)을 판정 단위로 간주하고, 운동 종목 정제(2026-08-08)의 선정 기준축을 "부위 × 동작패턴 × 기구"로 잡았다. 이는 저장·검색용 분류를 판정 단위로 착각한 것이다.

**2. 설계서 4.3의 균형 판정이 구현 불가능하다.**

4.3은 균형 판정의 계산 재료를 다음과 같이 명시하고 있다.

```
밀기   = 가슴 + 전면어깨 + 삼두
당기기 = 등 + 후면어깨 + 이두
하체   = 대퇴사두 + 햄스트링 + 둔근 + 종아리
```

즉 설계서가 요구하는 입자는 처음부터 `body_part` 6종보다 잘았다. `body_part = ARMS`로는 삼두를 밀기에, 이두를 당기기에 배분할 수 없고, `SHOULDERS`로는 전면·후면을 구분할 수 없다. 확정 기능인 균형 판정이 동작하지 않는다.

**3. 부족 판정도 왜곡된다.**

이두만 주 12세트, 삼두 0세트인 사용자에게 "팔 12세트, 최적" 판정이 내려진다. 삼두를 전혀 하지 않는데 충분하다고 알리는 것이므로, **약점을 놓치는 방향**의 오판이다. LOG-02(워밍업 제외)에서 결정적 근거로 삼았던 것과 동일한 성격의 문제다.

### 수정 버전

판정 단위를 `body_part`가 아니라 **`primary_muscle` 기반 근육군 9종**으로 정의한다. `body_part` 6종은 저장·검색·차트용으로 그대로 유지한다.

| 판정 부위 | 구성 `primary_muscle` | 종목 수 |
|---|---|---|
| CHEST | chest | 18 |
| BACK | lats, middle back, traps, lower back | 17 |
| DELT_FRONT | shoulders (오버헤드 프레스·전면 레이즈·측면 레이즈) | 10 |
| DELT_REAR | shoulders (후면 델트) | 3 |
| TRICEPS | triceps | 14 |
| BICEPS | biceps | 7 |
| QUADS | quadriceps, abductors, adductors | 11 |
| POSTERIOR | hamstrings, glutes | 7 |
| CORE | abdominals | 16 |
| **판정 대상 계** | | **103** |

**판정 제외 (표시만)**: calves 3, forearms 3

균형 판정에서의 취급:
```
하체   = 대퇴사두 + 햄스트링 + 둔근 + 종아리   ← 종아리 포함 (4.3 계산식 유지)
당기기 = 등 + 후면어깨 + 이두                  ← 전완 미포함 (4.3에 없음)
```

### 수정한 이유와 방식

**이유**: 위 세 문제 중 2번이 결정적이었다. 균형 판정은 설계서 5장에 "확정"으로 기재된 핵심 기능이며, 계산식이 이미 근육 단위를 전제하고 있었다. 따라서 이는 새로운 설계 결정이 아니라 **설계서에 암묵적으로 존재하던 단위를 명시화하는 작업**이다.

**방식**: 세 가지 선택지를 비교하였다.

| 선택지 | 판단 |
|---|---|
| `body_part` 6종 유지, 근육은 부가 설명으로만 사용 | 4.3의 균형 판정을 구현할 수 없다. 확정 기능을 포기하게 된다 |
| `body_part` enum을 근육 단위로 확대 | 계획서 4.4 스키마 변경 필요. 검색·차트 UI도 함께 재설계해야 한다 |
| **판정 단위와 저장 단위를 분리 (채택)** | 스키마 무변경. `primary_muscle`에 필요한 값이 이미 존재한다 |

CSV 7개 컬럼 중 어느 것도 재작성이 필요하지 않았다. 판정 로직이 읽는 컬럼만 달라진다.

**종아리·전완을 부족 판정에서 제외한 근거**

부족 판정은 "하지 않으면 문제"라는 진술이므로, ACSM 권고 단위에 해당하고 미수행이 실제 문제인 부위에만 적용해야 한다. 두 부위는 어느 조건도 충족하지 않는다.

- ACSM 권고는 큰 근육군 단위로 제시되며 종아리·전완을 별도 단위로 다루지 않는다.
- 다수의 사용자가 별도 훈련하지 않으며, 하지 않는다고 약점으로 부르기 어렵다.
- 포함할 경우 대부분의 사용자가 매주 "전완 부족" 경고를 받게 되어 경고의 신뢰도가 떨어진다.

설계서 0장 "근거로 방어할 수 없는 정밀도를 피한다"에 따른 판단이다. 오판 방향도 안전하다 — 약점을 놓치는 쪽이 아니라 언급하지 않는 쪽으로 틀린다.

**어깨 전면·후면 분리 방식**

free-exercise-db는 `primaryMuscles`를 `shoulders` 하나로만 제공하고 세부를 나누지 않는다. 따라서 종목 정제 시 부여한 동작 패턴을 기준으로 배분하였다.

| 패턴 | 배분 | 근거 |
|---|---|---|
| 오버헤드 프레스 | 전면 | `force = push` |
| 전면 레이즈 | 전면 | 명시적 |
| 측면 레이즈 | 전면 | 아래 별도 근거 |
| 후면 델트 | 후면 | 명시적 |

측면 레이즈(중간 삼각근)는 4.3의 계산식에 대응 항목이 없어 배정이 필요했다. 밀기로 배정한 이유는 두 가지다. 실제 훈련에서 어깨 프레스와 함께 밀기 세션에 배치되는 것이 일반적이며, 당기기에 넣을 경우 당기기 볼륨이 부풀려져 **당기기 부족을 놓치는 방향**으로 오판하게 된다.

**업라이트 로우 처리**

`body_part`는 SHOULDERS로 예외 지정하였으나(승모근이 주동근이지만 측면 삼각근을 타겟), 판정은 `primary_muscle = traps`를 따라 BACK으로 집계한다. 두 축이 어긋나지만, 업라이트 로우가 승모 상부를 주로 사용하는 것은 사실이므로 왜곡이 크지 않고, 예외 목록을 이중으로 관리하지 않는 편이 낫다고 판단하였다.

### 한계 인지

- **DELT_REAR가 3종목으로 얇다.** 판정과 후보정의 최소선(3개 기구 계열)은 충족하나, 후보 다양성이 부족하다. 후보 풀 402개에 후면 델트 종목이 더 존재하므로 보강 1순위로 둔다.
- **어깨 전·후면 배분은 원본 데이터에 없는 판단이다.** 동작 패턴 기반 추정이므로, 개별 종목 단위로 보면 이견이 있을 수 있다. 근거를 위 표에 명시하여 검증 가능하게 하였다.
- **판정 단위(9종)와 저장 단위(6종)가 다르다.** 화면에 표시되는 부위 분류와 판정 결과의 부위 명칭이 달라 사용자 혼란 가능성이 있다. 분석 결과 화면에서 명칭을 어떻게 노출할지는 시각화 설계 시 별도 검토한다. → **[LOG-10](#log-10)에서 2계층 구성으로 해소.**

---

<a id="log-10"></a>
## LOG-10. 판정·표시 부위의 2계층 구성

### 기존 버전

LOG-09에서 판정 단위를 `primary_muscle` 기반 9종으로 확정하였으나, 그 결과를 화면에 어떻게 노출할지는 정하지 않고 한계 인지 항목으로 남겨두었다.

> 판정 단위(9종)와 저장 단위(6종)가 다르다. 분석 결과 화면에서 명칭을 어떻게 노출할지는 시각화 설계 시 별도 검토한다.

8월 3주에 제작한 프론트엔드 목업은 홈·분석·통계 화면 전체를 `body_part` 6종으로 구성하였다. 미해결 항목에 대해 "6종으로 표시한다"는 답을 낸 상태였다.

### 기존 버전의 문제점

**1. 균형 판정이 설계서대로 동작하지 않는다.**

목업의 계산식은 다음과 같았다.

```
밀기   = 가슴 + 어깨 전체 + 팔 × 0.5
당기기 = 등 + 팔 × 0.5
```

설계서 4.3이 정한 `밀기 = 가슴 + 전면어깨 + 삼두`, `당기기 = 등 + 후면어깨 + 이두`와 다르다. 6종만으로는 삼두를 밀기에, 이두를 당기기에 배분할 수 없어 0.5씩 기계적으로 나누는 근사치를 쓴 것이다.

이 계수 0.5는 근거가 없다. 설계서 0장의 "근거로 방어할 수 없는 정밀도를 피한다"에 어긋나며, 실제로도 이두만 수행한 사용자와 삼두만 수행한 사용자가 동일한 균형 판정을 받는다. 후면 어깨 볼륨이 밀기로 집계되어 **당기기 부족을 놓치는 방향**의 오판도 발생한다.

**2. 부족 판정이 LOG-09가 지적한 상태로 되돌아간다.**

이두 12세트·삼두 0세트인 사용자에게 "팔 12세트, 최적"이 표시된다. 약점을 놓치는 방향의 오판이며, LOG-02에서 결정적 근거로 삼았던 것과 동일한 성격의 문제이다.

**3. 그렇다고 9종을 그대로 노출하기도 어렵다.**

"전면 삼각근", "후면 삼각근" 같은 명칭은 일반 사용자가 구분하지 못한다. 계획서 7장의 실사용자 검증은 지인 약 10명을 대상으로 하는데, 화면 용어를 이해하지 못하면 만족도 평가 자체가 유효하지 않게 된다.

### 수정 버전

판정 단위 9종을 유지하되, 화면은 **표시 6종(상위) — 판정 9종(하위)** 의 2계층으로 구성한다.

| 표시 부위 (상위) | 판정 부위 (하위) | 화면 표기 |
|---|---|---|
| 가슴 | CHEST | 가슴 |
| 등 | BACK | 등 |
| 어깨 | DELT_FRONT / DELT_REAR | 어깨(앞) / 어깨(뒤) |
| 팔 | TRICEPS / BICEPS | 삼두 / 이두 |
| 하체 | QUADS / POSTERIOR | 앞허벅지 / 뒤허벅지·둔근 |
| 코어 | CORE | 코어 |

- 상위 6종은 `body_part`와 일치하므로 종목 검색·필터·차트 분류에 그대로 쓴다.
- 하위 9종이 실제 판정 단위이며, 4단계 부족 판정과 균형 계산은 모두 이 단위로 수행한다.
- 상위가 하위 하나만 갖는 가슴·등·코어는 펼침이 없다. 계층이 갈리는 것은 **어깨·팔·하체 3개뿐**이다.
- 종아리·전완은 판정 대상이 아니므로 계층에 포함하지 않고 별도 "표시만" 영역에 둔다.

**라벨 규칙**: 상위 항목에는 판정 라벨을 붙이지 않는다. 세트 수만 표시하고 부족/권장 이하/최적/과다 판정은 하위에만 붙인다. 하위가 둘인 부위는 상위에 요약 배지(예: "일부 부족")를 표시한다. 상세는 「운동 분석 로직 설계서」 2.4에 둔다.

균형 판정 계산식은 설계서 4.3을 그대로 사용한다.

```
밀기   = CHEST + DELT_FRONT + TRICEPS
당기기 = BACK + DELT_REAR + BICEPS
상체   = CHEST + BACK + DELT_FRONT + DELT_REAR + TRICEPS + BICEPS
하체   = QUADS + POSTERIOR + 종아리
```

### 수정한 이유와 방식

**이유**: 판정 정확도와 사용자 이해도가 충돌하는 상황이었다. 9종을 그대로 노출하면 정확하지만 이해되지 않고, 6종으로 판정하면 이해되지만 확정 기능인 균형 판정이 성립하지 않는다. 계층을 나누어 **판정은 9종으로 정확하게, 요약 표시는 6종으로 익숙하게** 처리함으로써 양쪽을 함께 만족시킨다.

**방식**: LOG-09가 "판정 단위와 저장 단위를 분리"를 채택한 논리를 화면에 연장한 것이다. LOG-09가 스키마 수준에서 두 단위를 분리했다면, 본 항목은 화면 수준에서 같은 분리를 수행한다. 새로운 원칙을 도입하지 않으므로 근거를 별도로 세울 필요가 없다.

세 선택지를 비교하였다.

| 선택지 | 판단 |
|---|---|
| 판정·표시 모두 9종 | 설계서와 완전 일치하나, 명칭의 이해도가 낮아 실사용자 검증이 흐려진다 |
| 판정·표시 모두 6종 | LOG-09를 3주 만에 되돌리는 결정이며, 균형 판정이 근거 없는 계수에 의존하게 된다 |
| **판정 9종 · 표시 2계층 (채택)** | 판정 정확도를 유지하면서 요약 화면의 이해도를 확보한다. 계층이 갈리는 부위가 3개뿐이라 화면 부담이 작다 |

**발견 경위**: 본 항목은 8월 3주 프론트엔드 목업을 설계 문서와 대조하는 과정에서 드러났다. 목업이 미확정 사항(화면 표시 단위)에 대해 암묵적으로 답을 내고 있었고, 그 답이 확정 기능과 충돌하는 것을 확인하여 명시적 결정으로 승격하였다.

### 한계 인지

- **상위 항목의 세트 수는 하위의 단순 합이다.** 사용자가 상위 숫자만 보고 판단할 경우 LOG-09가 지적한 오해("팔 12세트니까 충분하다")가 여전히 발생할 수 있다. 라벨 규칙으로 완화하되 완전히 제거되지는 않는다.
- **QUADS의 화면 명칭이 부정확하다.** 이 부위는 `quadriceps` 외에 `abductors`·`adductors`를 포함하므로 "앞허벅지"라는 표기가 정확하지 않다. 해당 종목 수가 적어 실사용 영향이 작다고 보고 감수하되, 종목 정제 결과를 확인한 뒤 재검토한다.
- **요약 배지의 문안이 미확정이다.** 하위 둘이 서로 다른 방향으로 벗어난 경우(한쪽 부족·한쪽 과다)의 표기를 포함해 화면 구현 시 확정한다.

---

<a id="log-11"></a>
## LOG-11. 프로필 항목에서 운동 경력 제외

### 기존 버전

유스케이스 정의서 UC-01-03은 프로필 설정 항목을 **"키 / 체중 / 경력 / 목표"** 로 명시하였고, 기능명세서 1장 기본 기능 표의 프로필 행도 "유저 기본 정보(키/체중/운동 경력/목표)"로 적었다. 즉 운동 경력(`experience_level`)이 프로필 필드로 계획되어 있었다.

`V1__init.sql`의 `users`에는 프로필 컬럼 자체가 없어(`email / password_hash / nickname`만), 9월 인증 구현과 함께 프로필 컬럼을 추가할 예정이었다. 9월 API 명세 초안도 `experience_level`을 `BEGINNER / INTERMEDIATE / ADVANCED` enum으로 포함하고 있었다.

### 기존 버전의 문제점

**1. 경력은 분석·추천 어디에도 입력으로 쓰이지 않는다.** 부족·불균형 판정은 ACSM 기준(주당 세트 절대 임계)을 그대로 적용하며, 사용자 경력에 따라 임계를 바꾸지 않는다(「운동 분석 로직 설계서」 0장·2장). 추정 1RM과 강도 %도 기록에서 계산되어 경력과 무관하다.

**2. `goal`과 달리 예정된 사용처도 없다.** `goal`은 「운동 분석 로직 설계서」 1.3이 "강도 권고가 의미를 가지려면 목표(근력/근비대) 입력이 필요"라고 적어, 강도 권고를 도입하면 읽게 될 항목으로 근거가 있다. 경력에는 그런 구체적 사용 계획이 없다.

**3. 쓰이지 않는 항목은 비용만 늘린다.** 컬럼·API 필드·입력 폼·검증이 한 항목씩 늘고, 실사용자 검증(지인 약 10명)에서 결과에 영향을 주지 않는 입력을 요구하게 된다. 「운동 분석 로직 설계서」 0장 "근거로 방어할 수 없는 정밀도를 피한다"와 같은 태도로, 근거 없는 항목을 미리 두지 않는다.

### 수정 버전

- 프로필 항목을 **키 / 체중 / 목표 3종**으로 확정한다. `users`에 `height_cm` / `weight_kg` / `goal` 3컬럼만 추가한다(API 명세서 2.4-(2)).
  → **LOG-13에서 키·체중까지 제외해 훈련 목표 1종으로 축소되었다.** 본 항목이 세운 기준("예정된 사용처가 있는가")을 키·체중에도 적용한 결과이며, 아래 「수정한 이유와 방식」의 비대칭 논거가 그대로 근거가 되었다.
- `experience_level` 컬럼은 만들지 않는다. 필요해지면 그때 사용처와 근거(경력별로 무엇을 다르게 할지)를 갖춰 별도 마이그레이션으로 추가한다.
- `goal` enum은 훈련 목표 축으로 둔다 — `STRENGTH / HYPERTROPHY / ENDURANCE / GENERAL_FITNESS`. 목업의 체중 목표(증량/유지/감량)와는 축이 다르며, 목업을 훈련 목표로 교체한다.

### 수정한 이유와 방식

**이유**: LOG-09·LOG-10과 같은 결의 정리이다 — "쓰이지 않는데 자리를 차지하는 것"을 없앤다. 방향만 반대로, 이번에는 항목을 빼는 쪽이다.

**방식**: `goal`과 경력을 같은 기준("예정된 사용처가 있는가")으로 비교하여 `goal`은 남기고 경력만 제외하였다. 스키마를 늘리는 결정은 되돌리기 어렵고(데이터가 쌓이면 특히), 빼는 결정은 나중에 근거를 갖춰 추가하기 쉽다는 비대칭도 고려하였다.

**발견 경위**: 9월 API 명세서를 작성하며 `users` 프로필 컬럼을 확정하는 과정에서, "경력을 실제로 어느 로직이 읽는가"를 물었을 때 사용처가 없음이 드러났다.

**계획서·문서 반영**: 계획서(`.docx`)는 계획 시점 기록으로 보존한다. 유스케이스 정의서 UC-01-03과 기능명세서 1장은 프로필 항목을 "키/체중/목표"로 갱신한다.

### 한계 인지

- 경력이 있으면 초심자에게 더 보수적인 볼륨 목표를 제시하는 등의 개인화가 가능하나, 그런 로직을 설계한 바 없다. 현 시점에서는 근거 없는 개인화이므로 배제한다.
- 8월 3주 목업에는 경력 입력이 없어 목업 수정은 불필요하다. UC-01-03 문구와만 어긋나므로 해당 유스케이스를 갱신한다.

---

<a id="log-12"></a>
## LOG-12. 목업 전수 대조로 확정한 미규정 사항

### 기존 버전

8월 3주 목업과 8월 말 확정된 설계 문서가 서로를 참조하며 만들어졌으나, **전체를 맞대어 확인한 적은 없었다.** 그때까지 파악된 어긋남은 API 명세서 9.2의 3건(미래 날짜 화면, 종목 재배치, 프로필 목표 축)뿐이었다.

### 기존 버전의 문제점

9월 구현 착수 직전에 목업 전체(3,212줄)를 API 명세서·분석 로직 설계서·기록 방식 설계서·기능명세서와 대조한 결과, 두 종류의 문제가 드러났다.

**(1) 문서에 사실 오류가 있었다.**

- API 명세서 9.2가 *"'어깨(앞)/어깨(뒤)' 2계층 표시는 목업 그대로 유지된다(명세가 목업에 맞췄음)"*고 기록했으나 사실이 아니었다. 해당 문자열이 목업 전체에 0건이고, 목업 분석 탭은 상위 6종에 판정 라벨을 직접 붙이는 방식이다. 이는 **LOG-10이 금지한 표시 방식**이다. 즉 명세가 존재하지 않는 목업 기능에 맞췄다고 기록한 셈이며, 이 문장을 믿고 9월에 구현하면 2계층 리스트가 이미 있는 줄 알고 넘어가게 된다.
- 중량 정밀도가 1.2("소수 1자리")와 6.4("소수 2자리")로 문서 내부에서 모순이었다. 1.2는 같은 줄에서 `NUMERIC(6,2)`를 인용해 그 안에서도 어긋났다.

**(2) 화면을 그리려면 필요한데 어디에도 정해지지 않은 사항이 있었다.**

특히 주 시작 요일은 규정이 없어 목업이 홈=월요일 / 캘린더=일요일로 이미 갈라져 있었고, 하루 세션 수는 API 명세서 6.1("하루 한 번")과 6.8(`sessionCount: 2`)이 서로 충돌하고 있었다.

### 수정 버전

미규정 사항 5건을 확정했다.

| 항목 | 결정 | 근거 |
|---|---|---|
| 하루 세션 수 | **복수 허용.** `DRAFT` 단일 제약은 유지 | 오전·오후로 나눠 운동하는 사례가 실재하고, 한 세션에 몰아넣으면 중간 공백이 LOG-03의 15분 캡으로 잡혀 운동 시간이 왜곡된다 |
| 주 시작 요일 | **월요일** (ISO 8601). API 명세서 1.2에 규정 신설 | 표준이고 주간 루틴 관행(월·수·금 분할 등)과 맞는다. 분석의 4주 집계는 요일과 무관한 28일 롤링이라 영향받지 않는다 |
| `setNo` 재부여 | **하지 않는다.** 삭제 후 구멍이 생겨도 그대로 둔다 | 재부여하려면 남은 세트마다 PATCH를 돌리거나 삭제 응답에 갱신 목록을 실어야 해서 계약이 무거워진다. 화면이 배열 순번으로 그리면 사용자에게는 1, 2, 3으로 보인다 |
| 진행 중 세션 경과 시간 | **클라이언트가 `startedAt` 기준으로 계산해 표시.** "자동 기록" 표기 금지 | 서버는 종료 시점에만 `durationSec`을 산출한다(LOG-03). 클라이언트 표시값은 캡·보정이 없어 서버 값과 다르므로, 서버 기록값으로 읽힐 문구를 쓰지 않는다 |
| 부위별 볼륨 주간 추이 API | **10월로 이관.** 9월엔 화면도 제외 | 분석 탭의 핵심은 현재 약점 판정이지 추이가 아니다. 엔드포인트 신설 + 차트 6개 구현은 9월 범위에 비해 무겁다 |

사실 오류 3건은 각 문서에서 정정했고, 9.2의 「목업 수정 필요」 표를 3건 → 15건으로 확장했다.

### 수정한 이유와 방식

**이유**: 데이터 구조와 달리 이 항목들은 되돌리기 어려운 성격은 아니지만, **정하지 않으면 화면을 그릴 수 없다.** 주 시작 요일이 없으면 홈의 "이번 주"와 캘린더가 서로 다른 주를 가리키고, 하루 세션 수가 불명확하면 캘린더 셀 구조 자체가 정해지지 않는다. 목업이 이미 두 갈래로 갈라져 있던 것이 그 증거다.

**방식**: 목업 전체를 문서 4종과 대조해 격차를 전수 확인했다. 새로 찾은 어긋남은 37건(심각도 높음 9, 중간 19, 낮음 9)이며, 항목별 위치·근거·조치는 「목업_문서_대조_평가.md」에 정리했다. 그중 문서 쪽 문제 8건을 본 로그의 결정으로 처리했다.

**파급**: 하루 복수 세션을 확정하면서 "하루 한 번"이라는 기존 표현이 새 모순이 되었다. 기능명세서 1.1, 본 로그 LOG-01, 기록 방식 설계서 1장·2.1 네 곳을 함께 수정했다.

**대조 대상 보존**: 평가 보고서는 목업의 줄 번호로 근거를 지목한다(66곳). 원본은 23MB(99%가 폰트 base64)라 커밋하지 않고 로컬에 별도 보관한다. 원본은 전체가 이스케이프된 한 줄이라 줄 번호가 성립하지 않으므로, 보고서의 줄 번호는 폰트를 걷어내고 이스케이프를 푼 **정제본(3,212줄) 기준**이다. 재현 절차는 「목업_문서_대조_평가」 서두에 적어 두었다.

**한계 인지**: 심각도 중간·낮음 28건은 이번에 결정하지 않았다. 대부분 목업 수정 사항이라 9월 화면 구현 시점에 처리하는 것이 자연스럽고, 지금 확정해도 구현 전까지 검증할 방법이 없다.

---

<a id="log-13"></a>
## LOG-13. 구현 착수 전 계약 검토로 확정·정정한 사항

### 기존 버전

9월 백엔드 구현 착수 시점에 설계 문서 9종·API 명세서·프론트엔드 뼈대·환경 구성이 모두 갖춰져 있었다. 직전에 LOG-12가 목업과 문서를 전수 대조해 격차를 해소했으므로 산출물 간 정합성은 확인된 상태로 보았다.

### 기존 버전의 문제점

LOG-12가 대조한 축은 **목업 ↔ 문서**였다. **문서 ↔ API 계약** 축은 한 번도 맞대어 본 적이 없었다. 구현 착수 직전 이 축을 확인한 결과, "설계 문서끼리는 서로 맞는데 계약이 그것을 받쳐주지 못하는" 종류의 구멍이 드러났다.

**1. 확정 기능인데 그 값을 줄 API가 없었다.**

- 3단 프리필 ①(종목 선택 시 직전 기록 자동 채움)은 「운동기록_방식_설계서」 3.4·8장에서 확정이고 목업에도 구현돼 있으며, LOG-12의 대조 평가는 이를 "문서와 잘 맞는 부분 — 그대로 가져가도 되는 것"으로 분류했다. 그러나 API 명세서 25개 엔드포인트 어디에도 "이 종목의 직전 수행 세트"를 주는 경로가 없었다. 우회하려면 세션 목록(기본 최근 30일)을 훑고 세션마다 상세를 다시 부르는 N+1이 되고, 30일을 벗어난 종목은 찾지 못한다.
- 명세 5.2는 "최근 사용" 탭을 *"이력에서 최근 등장 `exerciseId` 순으로 계산"* 하도록 정했으나, 6.6 응답은 이름 배열(`exerciseNames`)만 주고 `exerciseId`가 없었다. 명세가 자기 안에서 모순이었다.
- 「기능명세서」 1이 히스토리의 기본 기능으로 명시한 "종목별 필터" 가 6.6의 파라미터에 없었다.
- 목업 계정 화면에 계정 삭제 버튼이 있으나(대조 평가 C-23) `DELETE /users/me`가 명세에 없었다. 지인 약 10명에게 개인정보를 받아 저장하면서 사용자가 스스로 지울 경로가 없는 상태였다.

**2. 집계 계산식이 28일이 아니라 29일을 세고 있었다.**

명세 8.2의 기간 조건 `performed_on ∈ [referenceDate - weeks×7일, referenceDate]`는 양끝을 포함하므로 **29일**이다. 그런데 주당 평균은 `weeks`(=4, 28일)로 나눈다. 모든 부위 볼륨이 약 3.6% 부풀려지고, 그 오차가 4 / 10 / 20 임계 바로 위에서 작동해 주 3.86세트인 사용자를 4.0으로 올린다. **약점을 놓치는 방향**이며, LOG-02(워밍업 제외)·LOG-09(판정 단위)가 일관되게 경계해온 오판 방향과 같다. 「운동기록_방식_설계서」 2.2의 예시 SQL(`>= CURRENT_DATE - INTERVAL '28 days'`)도 같은 오류를 갖고 있었다.

**3. 판정 경계값이 두 구간에 걸쳐 있었다.**

`4 ~ 10` / `10 ~ 20` 표기에서 정확히 `10.0`이 어느 구간인지 정해지지 않았다. `weeklySets`는 소수 1자리이고 4주 40세트 = 10.0은 흔히 나온다. LOG-12의 대조 평가는 `20` 경계는 잡았으나(C-29) `10`은 명세 자체가 미정이라 잡히지 않았다.

**4. 재전송이 세트를 중복 저장한다.**

요청이 서버에 도달해 저장까지 끝난 뒤 응답만 유실되는 경우가 있다. 클라이언트는 실패로 판단해 재전송하고, 멱등 키가 없으면 같은 세트가 2행이 된다. 중복 세트는 곧 볼륨 과대 집계이므로 2번과 같은 오판 방향이다. 오프라인 기능이 없어도 발생한다 — UC-02-01 예외 흐름 6a가 정한 "재시도를 안내한다"에서 사용자가 재시도를 누르면 그대로 일어난다.

**5. 네트워크 실패 시 보존 방침과 휴식 타이머 제외 근거가 각각 비어 있거나 어긋났다.**

- 6a는 "입력값을 화면에 유지"까지만 정했다. 그것이 메모리인지 로컬 저장소인지, 오프라인에서 어디까지 동작해야 하는지가 정해지지 않았다.
- 「운동기록_방식_설계서」 7장은 휴식 타이머를 *"분석에 사용되지 않는다"* 는 이유로 제외했다. 그러나 타이머는 데이터 수집 장치가 아니라 화면 보조 장치이므로 그 문장은 제외의 근거가 되지 못한다. 휴식 시간 자체는 이미 `recorded_at` 간격으로 들어오고 있고, 그 값을 판정에 쓸 수 없다는 것은 별개의 사실이다.

### 수정 버전

| # | 항목 | 결정 | 반영 위치 |
|---|---|---|---|
| 1 | 종목 프리필 API | `GET /exercises/{id}/last-performance` 신설. `DONE` 세션 중 `performed_on`이 가장 늦은 것에서 그 종목의 전 세트를 반환, 기록이 없으면 `204` | 명세 5.5, 기록 방식 3.4 |
| 2 | 히스토리 종목 식별 | 6.6 응답의 `exerciseNames` → `exercises: [{ id, nameKo }]` | 명세 6.6, 5.2 |
| 3 | 히스토리 종목별 필터 | 6.6에 `exerciseId` 파라미터 추가 (세션 단위 필터) | 명세 6.6 |
| 4 | 회원 탈퇴 | `DELETE /users/me` 신설. 비밀번호 재확인 후 `ON DELETE CASCADE`로 전량 삭제, 소프트 삭제 미사용 | 명세 4.7 |
| 5 | 집계 구간 | `[referenceDate - (weeks × 7 - 1)일, referenceDate]`. `referenceDate` 포함 정확히 `weeks × 7`일 | 명세 8.2, 분석 2.1, 기록 방식 2.2 |
| 6 | 판정 경계 | `x < 4` / `4 ≤ x < 10` / `10 ≤ x ≤ 20` / `x > 20`. **10과 20은 최적에 포함** | 명세 8.2, 분석 2.2, 기능명세 2.2, UC-03-01 |
| 7 | 세트 저장 멱등 키 | `clientSetId`(UUID) **필수**. `(session_id, client_set_id)` UNIQUE, 중복이면 `200 OK` + 기존 세트 반환 | 명세 6.4·6.5·6.2, `V4` 마이그레이션 |
| 8 | 오프라인 기록 | **온라인 전용.** 쓰기 큐를 두지 않는다 | 기록 방식 7.1 |
| 9 | 휴식 타이머 | 제외 해제. **표시 9월 / 알림 11월** 2단계. 저장하지 않으므로 서버 계약 변경 없음 | 기록 방식 3.7, 구축 기록 11.2 |

엔드포인트는 25개에서 27개가 되었다.

### 수정한 이유와 방식

**이유**: 다섯 항목의 성격이 다르지만 공통점이 있다. **어느 것도 실행해 보기 전에는 드러나지 않되, 실행한 뒤에는 되돌리기 비싸다는 것이다.** 1번은 세션 화면을 그리는 첫날 막히고, 2·3번은 판정 수치를 조용히 틀어놓으며(약점을 놓치는 방향이라 화면에는 아무 이상이 없다), 4번은 데이터에 중복이 쌓인 뒤에야 발견된다. LOG-12가 "정하지 않으면 화면을 그릴 수 없다"를 착수 전에 처리한 것과 같은 이유로, 계약의 구멍도 착수 전에 메웠다.

**방식**: LOG-12와 같은 전수 대조이되 축이 다르다.

| | LOG-12 | LOG-13 |
|---|---|---|
| 대조 축 | 목업 ↔ 설계 문서 | 설계 문서 ↔ API 계약 |
| 물음 | "화면이 문서대로 그려져 있는가" | "문서가 확정한 것을 계약이 실제로 줄 수 있는가" |
| 결과 | 신규 37건 + 문서 오류 8건 | 계약 구멍 4건 + 계산 오류 2건 + 미규정 3건 |

집계 구간(5번)은 문서만 보고 고치지 않고 **PostgreSQL에서 실측해 29일임을 확인한 뒤** 고쳤다. 판정 경계(6번)는 ACSM 권고("최소 유효 4세트, 최적 10~20세트, 20세트 초과 시 수익 감소")를 그대로 읽어 10과 20을 최적에 넣었다.

멱등 키(7번)를 선택이 아니라 필수로 둔 것은, 빠뜨려도 아무 오류가 나지 않고 중복이 조용히 쌓이기 때문이다. 클라이언트가 하나뿐인 현 단계에서 필수로 두는 비용은 `crypto.randomUUID()` 한 줄이다.

오프라인(8번)은 상용 헬스 기록 앱을 데이터를 끈 상태로 실행해 확인했다. "동기화가 실패했습니다" 안내와 함께 기록이 로딩되지 않았다 — 사용자가 네트워크 없이 기록 앱이 온전히 동작할 것을 전제하지 않는다는 확인이다. 노출 구간도 좁다: 신호가 있으면 세트는 체크 즉시 저장되므로, 손실은 *오프라인* 이면서 *새로고침·앱 종료*가 겹칠 때만 발생한다.

휴식 타이머(9번)는 제외 결론은 유지할 수 없었고 근거는 틀렸다는 두 가지를 함께 처리했다. 분석 활용 가능성을 따로 확인한 결과 판정에는 쓸 수 없음이 분명해졌으나(측정 신뢰도·문헌 합의 둘 다 부족), 그것은 타이머를 뺄 근거가 아니라 휴식 데이터를 판정에 쓰지 않을 근거였다. 서버 계약 변경이 0이고 화면 설계가 목업에 이미 있어 범위 안으로 들였다.

구현 지시 하나를 함께 남겼다: 타이머 표시를 `setInterval` 카운터가 아니라 `restStartedAt` 타임스탬프 기반으로 만든다. 브라우저가 백그라운드에서 타이머를 스로틀하므로 카운터 방식은 값이 어긋나고, 11월에 알림을 얹을 때 재작성이 된다. LOG-12가 진행 중 세션 경과 시간을 `startedAt` 기준 계산으로 정한 것과 같은 방식이다.

**파급**: `V4__add_client_set_id.sql`이 필요하다(7번). `V3`가 이미 적용된 뒤이므로 별도 버전으로 올린다.

### 한계 인지

- **이 검토는 문서와 코드를 읽은 결과이지 실행 검증이 아니다.** 화면을 실제로 붙이면 여기서 못 본 계약 구멍이 더 나올 수 있다.
- 오프라인 제외(8번)의 근거로 확인한 상용 앱 동작은 읽기(동기화·기록 로딩) 쪽이다. 쓰기를 로컬에 버퍼링하는 앱이 있을 수 있으므로 "상용 앱도 오프라인에서 저장이 안 된다"로 확대해 인용하지 않는다.
- 온라인 전용의 손실 단위는 세션 하나 전체다. 미완료 입력을 `localStorage`에 남겨 최악의 경우만 막는 방안(자동 재전송 없음)을 기록 방식 7.1에 "화면 구현 시 여유가 있으면"으로 남겼다.
- 휴식 타이머 표시(9번 A)는 화면을 보고 있을 때만 의미가 있다. 실사용 가치의 상당 부분은 11월 알림에서 나온다.
- `BACKFILL` 세션 생성은 여전히 재전송 중복이 가능하다(`LIVE`는 `DRAFT` 1개 제약이 막아준다). 같은 방식의 `clientSessionId`로 해결되며, 명세 6.2·9.1에 미결로 남겼다.

---

<a id="log-14"></a>
## LOG-14. 프로필 항목을 훈련 목표 1종으로 축소

### 기존 버전

[LOG-11](#log-11)에서 프로필 항목을 **키 / 체중 / 목표 3종**으로 확정하고, `users`에 `height_cm` / `weight_kg` / `goal` 세 컬럼을 두기로 했다. 운동 경력만 "예정된 사용처가 없다"는 이유로 제외했다.

### 기존 버전의 문제점

**LOG-11이 세운 기준을 남긴 항목들에는 적용하지 않았다.** 그 기준은 다음과 같았다.

> `goal`과 달리 향후 사용처도 구체적으로 예상되지 않는다. 쓰이지 않을 컬럼을 미리 두면 스키마만 늘어나므로, 필요해질 때 근거를 갖춰 추가한다.

같은 물음을 키·체중에 던지자 답이 경력과 다르지 않았다.

| 항목 | 현재 사용처 | 예상 사용처 |
|---|---|---|
| `height_cm` | 없음 | 없음. 어떤 로직도 참조하지 않는다. BMI 등 체중 관리 지표는 이 앱의 범위 밖이다 — 목표축을 체중 목표에서 훈련 목표로 교체한 것이 그 결정이었다 |
| `weight_kg` | 없음 | `WEIGHTED_BODYWEIGHT` 종목의 실제 부하 산출뿐. 그런데 해당 종목이 **109종 중 2종**(중량 풀업·중량 벤치 딥스)이고, 단일 스냅샷이라 과거 기록에 소급 적용하면 부정확하다. 실제로 쓰려면 체중 이력 테이블이 따로 필요하다 |
| `goal` | 없음 | 있음. 루틴 추천의 출력 형식이 "운동명·세트·**반복 횟수**·순서"(기능명세 2.1)인데, 반복 횟수와 강도 범위는 ACSM이 목표별로 다르게 제시한다. 목표를 모르면 정할 근거가 없다 |

즉 셋 중 `goal`만이 **출력 형식이 그 값을 요구하는** 경우였고, 나머지 둘은 경력과 같은 처지였다.

### 수정 버전

- 프로필 항목을 **훈련 목표 1종**으로 축소한다. `users`에 `goal` 한 컬럼만 둔다.
- `height_cm` / `weight_kg` 컬럼은 만들지 않는다. 필요해지면 그때 사용처와 근거를 갖춰 별도 마이그레이션으로 추가한다.
- API 응답의 `profile` 객체 구조는 유지한다. 항목이 하나여도 향후 확장 시 최상위 필드가 흩어지지 않게 하기 위함이다.

### 수정한 이유와 방식

**이유**: 판단 기준은 [LOG-08](#log-08)이 `equipment`를 미리 확보할 때 쓴 **비용 비대칭**이다.

> 비용이 비대칭이다. 지금은 컬럼 1개와 정제 시 매핑 1개이나, 나중에는 800여 종목 재검토가 필요하다

종목 데이터는 이 비대칭 때문에 미리 넣는 것이 옳았다. 그러나 **프로필 컬럼에는 그 비대칭이 없다** — 필요해지면 NULL 허용 컬럼 하나와 폼 필드 하나를 추가하면 되고, 소급해서 채워야 할 과거 데이터도 없다(어차피 스냅샷이라 이력이 존재하지 않는다). 미리 확보할 이유 자체가 성립하지 않는다.

여기에 하나가 더해진다. **실사용자 검증(지인 약 10명)에서 쓰지 않을 신체 정보를 수집하게 된다.** 안 쓸 것이면 받지 않는 편이 개인정보 수집 최소화 관점에서 맞다.

**방식**: LOG-11이 경력에 적용한 물음("실제로 어느 로직이 읽는가")을 나머지 두 항목에도 그대로 던졌다. 체중은 처음에 "사용처가 구체적으로 있다"고 판단했으나, 대상 종목 수를 실제로 세어 본 결과 109종 중 2종이었고 그마저 스냅샷 문제로 막혀 있어 판단을 뒤집었다.

**시점**: 제거 비용이 가장 싼 순간이었다. `V3` 마이그레이션이 아직 원격에 푸시되지 않았고, `users` 행이 0이며, 종목 시드가 반복 마이그레이션(`R__seed_exercises.sql`)으로 재현 가능했다. 따라서 `V5__drop_*` 같은 되돌림 마이그레이션을 만들지 않고 `V3` 자체를 수정하고 볼륨을 지워 빈 DB에서 재적용해 검증했다 — 스키마 이력에 "넣었다가 뺐다"가 남지 않는다. 이 창은 배포 시점에 닫힌다.

**계획서·문서 반영**: 계획서(`.docx`)는 계획 시점 기록으로 보존한다. 유스케이스 UC-01-03과 기능명세서 1장을 "훈련 목표"로 갱신하고, LOG-11에는 본 항목으로 승계되었다는 표시를 달았다.

**목업 파급**: 개인 정보 화면의 키·체중 표시가 제거 대상이 된다. 대조 평가 C-21이 *"하드코딩돼 있고 편집 불가 — 프로필 3종 중 2종을 입력할 화면이 없다"* 로 지적한 역방향 격차인데, 입력 화면을 만드는 대신 항목을 없애는 쪽으로 해소된다.

### 한계 인지

- `WEIGHTED_BODYWEIGHT` 2종목의 추정 1RM은 계속 산출하지 않는다. 체중을 받지 않기로 했으므로 「운동기록_방식_설계서」 2.3의 "체중 미반영으로 왜곡" 제외가 그대로 유지된다.
- `goal` 역시 현재는 쓰이지 않는다. 10월 루틴 추천에서의 사용 방식은 「루틴생성_로직_설계서」(미작성)에서 확정되며, 그 전까지는 "예상되는 사용처"에 머문다. 셋 중 유일하게 출력 형식이 그 값을 요구한다는 점이 남긴 근거다.
- 프로필이 한 항목으로 줄어 「기능명세서」 1의 기본 기능 "프로필"의 화면 분량이 작아진다. 근거 있는 축소이므로 감수한다.

---

<a id="log-15"></a>
## LOG-15. 배포 시점을 11월에서 10월 중순으로 조정

### 기존 버전

「개발환경_구축_기록」은 배포를 **11월**로 계획했다 — AWS EC2, 애플리케이션 컨테이너화(5.1), 서비스워커·오프라인 캐싱·FCM(11.2), 도메인·HTTPS(11.3)를 모두 그 시점에 두었다. 개발 일정은 9월 백엔드 → 10월 루틴 추천 → 11월 배포 순이었다.

### 기존 버전의 문제점

**1. 실사용자 검증 4주는 압축할 수 없는 벽시계 시간이다.**

「기능명세서」 5장의 검증 계획은 *"지인 약 10명 배포 → 루틴 만족도 / 실제 수행률 / **4주 후 부위 균형 개선 여부**"* 다. 코드를 빨리 짜서 줄일 수 있는 항목이 아니다. 배포가 11월이면 4주가 끝나는 것은 12월이고, 그 뒤에 데이터 정리와 보고서 작성이 남는다.

**2. 분석이 의미를 가지려면 기록이 먼저 4주 쌓여야 한다.**

판정 기간이 최근 4주(28일)이므로, 배포 직후의 사용자에게는 `confidence.level = LOW`만 뜬다. **기록 기능을 늦게 배포할수록 검증 기간의 데이터 품질이 나빠진다.**

**3. HTTPS가 PWA 선행 조건인 줄 알았으나 인증 선행 조건이었다.**

구축 기록 11.3은 HTTPS 리스크를 서비스워커·FCM 관점으로만 적었다.

> 서비스워커는 HTTPS에서만 등록된다... 도메인 확보와 Let's Encrypt 인증서 발급이 **PWA 완성**의 선행 조건

그러나 API 명세서 2.3이 정한 리프레시 토큰 쿠키는 `Secure` 속성을 갖는다. **`Secure` 쿠키는 http:// 오리진에서 브라우저가 저장하지 않으므로**(localhost만 예외), IP나 http로 배포하면 리프레시 토큰이 저장되지 않고 액세스 토큰 30분이 지나면 전원 로그아웃된다. 재로그인해도 30분 뒤 반복된다. PWA는 없어도 앱이 돌지만 **인증은 없으면 앱이 돌지 않는다** — 리스크 등급이 다르다.

### 수정 버전

- **10월 중순에 조기 배포한다.** 루틴 추천이 미완성인 상태로 **기록·분석까지만** 먼저 배포하고, 이후 완성되는 대로 업데이트한다.
- 배포 선행 작업을 **9월 말**로 당긴다.

| 항목 | 배포 전 필수인 이유 |
|---|---|
| 도메인 + Let's Encrypt | `Secure` 쿠키 미저장으로 **로그인 자체가 불가** |
| nginx 리버스 프록시 + SPA 폴백 | 동일 오리진 구성(구축 기록 10.2) + `try_files`(deep link 404 방지) |
| `application-prod` 프로필 분리 | actuator `show-details` 하향, DB 접속 정보 주입 |
| `Dockerfile` / CD | 구축 기록 5.1이 "11월 배포 준비 시"로 잡아둔 것 |
| PWA 아이콘 192 / 512 | 현재 manifest의 `icons: []`로는 설치 프롬프트가 뜨지 않는다 |

- 뱃지·친구 그룹은 「기능명세서」 4장의 우선순위를 **그대로 유지**하되, 조기 배포 대상이 기록·분석까지이므로 배포를 막지 않는다. 순서는 `배포 인프라(9월 말) → 배포(10월 중순) → 루틴 추천 → 뱃지·그룹`이 된다.

### 수정한 이유와 방식

**이유**: 지도교수 피드백의 단계 구성과도 맞는다.

> 1. 운동 기록 방식을 구체화해 충실한 기록을 확보할 것
> 2. 기록을 바탕으로 다양한 측면의 분석을 제공할 것
> 3. 마지막으로 부족한 부분을 추천하도록 구성할 것

10월 중순이면 단계 1·2가 끝나 있다. **단계 3이 끝나기를 기다렸다가 한 번에 배포하는 것보다, 1·2를 먼저 내보내 기록을 쌓게 하는 편이 권고와 정합적이다.**

**방식**: 세 가지 선택지를 비교하였다.

| 선택지 | 판단 |
|---|---|
| **10월 중순 조기 배포 (채택)** | 검증 기간 확보 + 기록 축적 시간 확보. 루틴 추천은 업데이트로 제공 |
| 10월 말 (루틴 1차 완성 후) | 검증 기간 4~5주는 확보되나 루틴 만족도 데이터가 얇아진다 |
| 11월 계획 유지 | 4주 검증이 12월로 넘어가 데이터 정리·보고서 작성과 겹친다 |

**파급**: 「개발환경_구축_기록」 5.1·11장·13장이 배포를 11월 전제로 서술하고 있어 갱신이 필요하다. 11월은 배포가 빠진 대신 서비스워커·오프라인 캐싱·FCM·휴식 알림(LOG-13 9번 B단계) 묶음에 집중하게 된다.

### 한계 인지

- 9월 말에 배포 인프라가 더해지면서 **9월이 무거워진다.** 백엔드 27개 엔드포인트·인증·분석 로직·화면 전부(현재 Placeholder)와 겹친다. 밀릴 경우 가장 먼저 미룰 수 있는 것은 휴식 타이머 표시(LOG-13 9번 A)이며, 그것은 다른 결정에 영향을 주지 않는다.
- 미완성 상태로 배포하므로 초기 사용자는 루틴 추천을 볼 수 없다. 검증 참여자에게 기능이 단계적으로 추가된다는 점을 미리 안내해야 한다.
- 조기 배포는 되돌릴 수 없다 — 실사용 데이터가 쌓이기 시작하면 스키마 변경 비용이 급격히 오른다. LOG-13·LOG-14의 계약·스키마 정리를 배포 전에 끝낸 것은 이 때문이다.

---

## 문서별 반영 현황

| 문서 | 반영 상태 | 반영 내용 |
|---|---|---|
| `운동기록_방식_설계서.md` | **신규 작성** 후 개정 | LOG-01 ~ LOG-07 전체의 상세 설계, LOG-08(2.3·8장·10장). LOG-13(2.2 예시 SQL 집계 구간 정정, 3.4 프리필 데이터 원천 연결, **3.7 휴식 타이머 신설**, **7.1 오프라인 온라인 전용 신설**, 7장 제외 표·8장 결정 요약 갱신) |
| `운동_분석로직_설계서.md` | 2판으로 개정 | LOG-02(1.2), LOG-07(1.3), 집계 조건(2.3), 결정 요약(5장), 7.2 완료 처리, 8장 갱신(LOG-08 포함). LOG-09 판정 부위 9종 정의(2.1·2.3·4.1·4.3·5장), 균형 판정 계산식을 판정 부위 표기로 교체, 어깨 전·후면 배분 근거 및 미해결 사항 명시. LOG-10 표시 계층 정의(2.4 신설), 결정 요약 행 추가. LOG-13(2.1 집계 구간 28일 명시, 2.2 판정 경계 부등호 표기) |
| `기능명세서.md` | 개정 | LOG-01·03(1장, 2.6), LOG-02(2.2), LOG-05(5장), LOG-07(2.2), LOG-08(2.4), LOG-10(2.2), LOG-11·LOG-14(1장 프로필 항목을 「훈련 목표」 1종으로 축소 + 근거 주석), LOG-13(2.2 판정 경계 표기) |
| `유스케이스_정의서.md` | 개정 | UC-02-01 상세 명세 신설, UC-02-06 신설, UC-03-01 집계 조건, UC-03-05 후보 선정 기준(LOG-08), LOG-11·LOG-14(UC-01-03을 「프로필 설정 (훈련 목표)」로 갱신 + 축소 근거 주석), LOG-13(UC-03-01 판정 경계 표기) |
| `API_명세서.md` | **신규 작성** (2026-08-30) | 9월 1~3주 구현분(인증·프로필 / 운동 기록 / 통계·분석) 엔드포인트 명세. LOG-09·LOG-10의 판정 2계층을 응답 구조로 구현, LOG-05·LOG-02 등 기록 설계 결정 반영. 어깨 앞/뒤 분리를 위한 `exercises.delt_region` 컬럼(V3)·즐겨찾기 테이블 등 후속 스키마 정리. LOG-13(4.7 회원 탈퇴·5.5 종목 프리필 신설, 6.4 멱등 키, 6.6 `exercises[]`·종목별 필터, 8.2 집계 구간·판정 경계, 9.1 `V4` 항목 — 엔드포인트 25→27개), LOG-14(2.4-(2)·4.5·4.6 프로필을 `goal` 1종으로) |
| `LLM_연동_실험기록.md` | **신규 작성** (2026-08-30) | 8월 4주 계획 항목 "LLM API 사전 실험". ① 분석 해설(설계서 7.3)의 LLM 통제 검증 완료 — Google Gemini 무료 티어, 합성 케이스 10개 + 자동 검증 V1~V7 + 규칙 폴백. 판정 수치·라벨·부위명·처방 톤 위반 0. `gemini-3.5-flash-lite` 구조화 출력 숫자 직렬화 결함(~7.5%) 발견 → 규칙 폴백이 필수임을 입증. 실험 하네스는 `scripts/llm-experiment/`(실행 원본은 git 미추적). ② 루틴 생성 실험은 10월 |
| `개발환경_구축_기록.md` | **신규 작성** 후 개정 | 8월 2주 환경 구축 과정의 구현 결정과 근거. 설계 변경이 아닌 구현 기록. LOG-09 종목 정제 결과(0장 병행 과제). 이후 5~7단계 완료 및 9~13장(React(Vite) PWA 구성 등) 신설 반영. LOG-13(11.2에 휴식 타이머 알림 추가). LOG-15(0장 8단계 신설, 1.1·4.3·5.1·6.1·6.3·10.2 배포 시점 갱신, **11.3을 「HTTPS는 인증의 선행 조건」으로 개정**, 13.2 README 배포 섹션) |
| `루틴생성_로직_설계서.md` | **미작성** | 저장소에 아직 없음. 10월 루틴 추천 착수 전 작성 예정. 작성 시 LOG-08의 후보정 트리거·정렬 기준, ① LLM 실험 결과(재생성 N값·정량 재검증·규칙 폴백)를 반영한다 |
| `목업_문서_대조_평가.md` | **신규 작성** (2026-08-31) | 목업 전체(3,212줄)와 문서 4종의 전수 대조 결과. 신규 37건(높음 9·중간 19·낮음 9) + 문서 쪽 문제 8건. LOG-12의 근거 자료이며, 심각도 중간·낮음 28건은 9월 화면 구현 시 처리. 2026-09-01 목업 보관 위치·줄 번호 기준 절 추가. C-21(키·체중 편집 불가)은 LOG-14의 항목 제거로, C-23(계정 삭제 버튼 미연결)은 LOG-13의 `DELETE /users/me` 신설로 해소된다 |
| `밸런스핏 프로토타입.html` (목업) | **미커밋 (의도적)** | 23MB 중 99%가 폰트 base64라 커밋하지 않고 로컬 보관한다. 「목업_문서_대조_평가」의 줄 번호 인용 66건은 폰트를 제거한 정제본(3,212줄) 기준이며, 재현 절차는 해당 문서 서두에 있다. 9월 화면 구현 시 레이아웃 참조용으로 필요하다 |
| `설계변경_로그.md` | 반영 완료 | LOG-09·LOG-10·LOG-11 신설, LOG-09 한계 인지에 해소 표기. LOG-12 신설(2026-08-31), LOG-01 엔티티 표를 하루 복수 세션에 맞춰 수정. LOG-13·LOG-14·LOG-15 신설(2026-09-02), LOG-11 수정 버전에 LOG-14 승계 표기 |
| `졸업작품_계획서.docx` | **미반영 (의도적)** | 제출·승인이 완료된 문서이므로 계획 시점의 기록으로 보존한다. 판정 부위 정의(4.3)를 포함한 모든 변경 사항은 본 로그와 최종 보고서에 반영한다 |

---

## 최종 보고서 작성 시 활용 방법

계획서 8장은 예상되는 어려움을 미리 정리한 장이고, 최종 보고서에서는 실제로 무엇이 달라졌는지를 쓰게 된다. 본 로그의 각 항목은 다음과 같이 대응한다.

- **「계획과 달라진 부분」** — LOG-01, LOG-04, LOG-08이 계획서 4.4의 설계와 직접 달라진 부분이다. LOG-11·LOG-14는 계획서·유스케이스가 명시한 프로필 항목(경력·키·체중)을 제외해 훈련 목표 1종으로 줄인 것이다. LOG-15는 배포 시점을 11월에서 10월 중순으로 당긴 일정 변경이다.
- **"설계 과정에서 구체화한 부분"** — LOG-02, LOG-03, LOG-05, LOG-06, LOG-07은 계획서에서 정하지 않았던 사항을 확정한 것이다. LOG-09, LOG-10은 판정 부위의 단위와 표시 계층을 명시화한 것이다. LOG-12는 구현 착수 직전 목업과 문서를 전수 대조해 남은 미규정 사항을 확정하고 문서 자체의 오류를 정정한 것이다. LOG-13은 같은 작업을 API 계약 축으로 수행해 계약의 구멍과 계산식 오류를 메운 것이다.
- **「설계 검증 방법」** — 구현 착수 전에 **두 축의 전수 대조**를 수행하였다. LOG-12는 *목업 ↔ 설계 문서*(「화면이 문서대로 그려져 있는가」), LOG-13은 *설계 문서 ↔ API 계약*(「문서가 확정한 것을 계약이 실제로 줄 수 있는가」)을 대조하였다. 산출물이 여러 개로 늘어나면 서로 모순이 생기고(중량 정밀도·하루 세션 수), 문서끼리는 맞는데 계약이 받쳐주지 못하는 구멍도 생긴다(3단 프리필의 API 부재, 집계 구간 29일). 둘 다 구현 전에 잡아낸 사례로 활용할 수 있다. LOG-13의 집계 구간 오류는 문서 판단에 그치지 않고 **PostgreSQL에서 실측해 확인**하였다.
- **"판단 근거"** — 각 항목의 "수정한 이유와 방식"에 선택지 비교와 채택 근거가 기록되어 있으므로, 보고서에서 결정 과정을 설명할 때 그대로 활용할 수 있다.

---

*본 로그는 설계·구현 진행에 따라 계속 추가한다.*
